version: '3.9'

services:
  auth-service:
    build:
      context: .
      args:
        GO_IMAGE_NAME: golang
        GO_IMAGE_VERSION: 1.23
    container_name: auth-service
    networks:
      - bb-network
    environment:
      - SECRET_KEY=SECRET_KEY

networks:
  bb-network:
    external: true